name: Integration test

on:
  push:
    branches: [main]

jobs:
  integration:
    runs-on: ubuntu-latest
    name: Integration Test
    steps:
      - name: Checkout fixtures
        uses: actions/checkout@v2
        with:
          ref: fixtures
          
      - name: Create bulk issues
        uses: benbalter/bulk-issue-creator@main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CSV_PATH: ./spec/fixtures/data.csv
        with:
          csv_path: ${{ env.GITHUB_WORKSPACE }}/spec/fixtures/data.csv
          template_path: ${{ env.GITHUB_WORKSPACE }}/spec/fixtures/template-with-front-matter.md.mustache
